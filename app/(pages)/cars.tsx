import { StyleSheet, Text, View } from "react-native";
import React from "react";

const cars = () => {
  return (
    <View>
      <Text>Car Selection Page</Text>
    </View>
  );
};

export default cars;

const styles = StyleSheet.create({});
